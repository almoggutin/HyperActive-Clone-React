{
    "name": "hyperactive-clone",
    "version": "1.0.0",
    "description": "In this repository you will find the source code of the homepage of the Hyperactive website.",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "client": "cd client && npm start",
        "build": "cd client && npm run build",
        "dev": "cd client && npm start && npm sass",
        "start": "node server/index.js",
        "heroku-postbuild": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "chalk": "^4.1.2",
        "cors": "^2.8.5",
        "express": "^4.17.1"
    }
}
